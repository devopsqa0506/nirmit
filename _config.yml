# GitHub Pages Jekyll Config
plugins:
  - jekyll-remote-theme

# Custom Headers for Google Login
include:
  - _headers

# Allow popups for Google OAuth
webrick:
  headers:
    Cross-Origin-Opener-Policy: "same-origin-allow-popups"
    Content-Security-Policy: "script-src 'self' https://apis.google.com"
